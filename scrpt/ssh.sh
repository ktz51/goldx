ssh-keygen
eval `ssh-agent -s`
ssh-add

#git push out from repository cloning,transform repository in unread mode
#go back normal on repository cloning and push up..git add -A not git add .
#commit -a -m ""